import React from 'react'

const NewBlogPost = () => {
    return (
        <div>Under Construction</div>
    )
    }
    export default NewBlogPost
   /* const defaultValues = {
        city: 'e.g. Hamburg',
        country: 'e.g. Germany',
        description: 'e.g. Hamburg is the best city on earth',
        image: 'Image link',
        longitude:'e.g. 39.57163',
        latitude: 'e.g. 39.57163'
    }

    const [newBlogEntry, setNewBlogEntry] = useState(initialState)
}

const addCity = (event) => {
    const{value} = event.currentTarget
    set newBlogEntry({...newBlogEntry,value:city})
}

const addCountry = (event) => {
    const{value} = event.currentTarget
    set NewBlogEntry({...newBlogEntry,value:county})
}*/


